{
  "name": "robot-buddy-adventure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "simulate": "node tools/simulate.js"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.0",
    "rollup": "^4.0.0",
    "vitest": "^3.0.0"
  }
}
